// The chat UI is mounted once inside AppShell and persists across
// /chat ↔ /c/:id navigations. This page file is just the route marker;
// AppShell reads pathname and shows/hides the chat shell accordingly.
export default function ChatPage() {
  return null;
}
