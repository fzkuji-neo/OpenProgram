"""Private physical home for the legacy ``openprogram.webui`` namespace.

Modules in this directory are loaded through ``openprogram.webui`` so existing
imports and shared Server state keep one module identity during migration.
"""
