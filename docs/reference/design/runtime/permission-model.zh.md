# 执行身份、权限与沙箱

权限模型现已合并进唯一权威设计：[执行权限与沙箱设计](sandbox-architecture.zh.html)。

该设计将三个合同分开：

- **Authority** 定义请求由谁发起，以及其固定权限档的能力上限。
- **Permission** 决定一次具体工具操作是允许、需要 owner 审批还是拒绝。
- **Sandbox** 约束获准本地进程实际能够访问的宿主资源。

本页只作为源代码注释和旧设计链接的稳定入口保留，不是第二份设计正文。
