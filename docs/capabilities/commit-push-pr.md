# Commit, push, PR

OpenProgram no longer ships a default `commit-push-pr` skill or
`/commit-push-pr` command. Git operations remain available through ordinary
tools, subject to the normal permission policy and explicit authorization for
remote writes.

A reusable product workflow for this process should be implemented as a
Program. Users may also install their own skill if they only need instructions.
