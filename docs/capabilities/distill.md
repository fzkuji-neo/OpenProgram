# Distill

OpenProgram no longer ships a default `distill` skill or `/distill` command.
The retained `read_conversation` tool can expose an earlier session to a
Program or to a skill installed by the user, project, or plugin.

Reusable product workflows belong in Programs. Users who prefer reusable model
instructions can create or install their own AgentSkills-compatible `SKILL.md`.
