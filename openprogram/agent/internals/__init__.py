"""Agent package internals — implementation modules used by the
dispatcher / agent_loop and a few webui/cli call sites. Grouped here
(out of the flat agent/ dir) so the package root lists its public
surface, not its private parts. Import as
``openprogram.agent.internals.<name>``.
"""
