"""Channel implementations — one module per external chat platform
(Discord / Slack / Telegram / WeChat). Each subclasses
``openprogram.channels.base.Channel``. Grouped here so channels/ root
shows the framework (base/setup/worker/accounts/bindings), not the
per-platform code.
"""
